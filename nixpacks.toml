# Nixpacks configuration for Coolify deployment
[phases.install]
cmds = ["npm ci --ignore-engines || npm install --no-audit"]

[phases.build]
cmds = ["npm run build --workspace=@camp-platform/web"]

[start]
cmd = "cd packages/web && npm run start"

[variables]
NODE_ENV = "production"
